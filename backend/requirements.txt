# Backend dependencies for Indian Car Visual Comparator API
fastapi==0.115.0  # Modern Python web framework
uvicorn[standard]==0.34.0  # ASGI server (recommended)
pydantic==2.10.3  # Data validation
httpx==0.28.1  # HTTP client for API calls
pillow==11.1.0  # Image processing utilities

# Optional: For future features
# sqlalchemy==2.0.36  # Database ORM (if needed beyond SQLite)
# redis==5.2.1        # Caching layer (if Redis installed)
